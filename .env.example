# PostgreSQL (Neon recomendado — local e Vercel)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

JWT_SECRET="change-me-to-a-long-random-string-min-32-chars"

# Seed (primeira vez: npm run db:push && npm run db:seed)
ADMIN_EMAIL="admin@sistema.local"
ADMIN_PASSWORD="rmbvadmin"
ADMIN_NAME="Admin"
ADV_EMAIL="adv@sistema.local"
ADV_PASSWORD="Adv@123"
ADV_NAME="ADV"
GERENTE_EMAIL="gerente@sistema.local"
GERENTE_PASSWORD="Gerente@123"
GERENTE_NAME="Gerente"

# Vercel Blob — Storage no painel Vercel → Storage → Blob (produção)
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# Datajud (CNJ) — Acompanhamento processual via API oficial
# DATAJUD_API_KEY="sua-chave-aqui"
# DATAJUD_BASE_URL="https://datajud.cnj.jus.br/api/v1"

# Google Sheets — Webhook para registrar solicitações de suporte
# Crie um Apps Script na planilha (docs/google-apps-script-webhook.gs) e cole a URL aqui
# SUPORTE_SHEET_WEBHOOK_URL="https://script.google.com/macros/s/SEU_ID/exec"
